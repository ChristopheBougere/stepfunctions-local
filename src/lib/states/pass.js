const State = require('./state');

class Pass extends State {
  // TODO: Add PASS_STATE_ENTERED event to execution's history in constructor
  // TODO: Add PASS_STATE_EXITED event to execution's history when finished
}

module.exports = Pass;
